<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/txtRenameLabel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Recording Name:"
        android:textAppearance="?android:attr/textAppearanceLarge" />

    <EditText
        android:id="@+id/edtRenameRecording"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10" >

        <requestFocus />
    </EditText>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" >

        <Button
            android:id="@+id/btnRenameRecordingCancel"
            style="@style/RenameButton"
            android:text="Cancel" />
        
       <Button
            android:id="@+id/btnDeleteRecording"
            style="@style/RenameButton"
            android:text="Delete" />

        <Button
            android:id="@+id/btnRenameRecording"
            style="@style/RenameButton"
            android:text="Rename" />
        
    </LinearLayout>

</LinearLayout>